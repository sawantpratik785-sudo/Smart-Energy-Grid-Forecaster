# Python
__pycache__/
*.py[cod]
*$py.class

# Virtual Environments
venv/
env/
.env
.venv

# IDE / OS Files
.vscode/
.idea/
.DS_Store
Thumbs.db

# Note: We are keeping the /data/ and /models/ folders un-ignored
# because the CSV (497 KB) and Joblib models (max 24 MB) are well 
# within GitHub's 100 MB file limit and are required for the app to run.
